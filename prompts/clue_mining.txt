请根据新信访件与候选历史案件，识别可能的新线索，并输出严格合法 JSON。

要求：
1. 只输出 JSON，不要输出 Markdown。
2. 仅保留有明确价值的新线索；如果没有，就返回空数组。
3. 线索类型仅使用：人物、金额、时间、关系、行为、项目。
4. `risk_level` 仅使用：高、中、低。
5. 如果候选案件与新案件不构成重复件，也请返回空数组。

新案件：
{new_case}

候选案件：
{candidates}

输出格式：
{{
  "new_clues": [
    {{
      "clue_type": "关系",
      "description": "候选案件提到被举报人通过亲属承揽附属工程，可作为延伸核查线索。",
      "risk_level": "高"
    }}
  ]
}}
